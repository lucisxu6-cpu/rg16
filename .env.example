# Set to 1 to show paid content without Stripe (local dev only).
DEV_BYPASS_PAYWALL="0"

# Local file store (optional).
RG16_STORE_PATH=""

# Public base URL of the app (used for Stripe redirect URLs).
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Stripe keys (required for real payments).
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
